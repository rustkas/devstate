[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "devstate-client"
version = "0.0.0"
description = "DevState Python client generated from OpenAPI"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [{name = "rustkas"}]

[tool.devstate]
# Version will be replaced from Git tag in CI